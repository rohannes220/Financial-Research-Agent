services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: financial_agent
    ports: ["5432:5432"]
    volumes: [financial_agent_pg:/var/lib/postgresql/data]
volumes:
  financial_agent_pg:
